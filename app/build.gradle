plugins {
    id "com.android.application"
}

android {
    namespace "dev.codex.voyahhud"
    compileSdk 35

    defaultConfig {
        applicationId "dev.codex.h53direct"
        minSdk 23
        targetSdk 28
        versionCode 56
        versionName "5.6"
    }
}

dependencies {
    implementation fileTree(dir: "libs", include: ["*.jar"])
}
